$(function() {
  Util.setActiveNav();

  // Textile the original content
  // $('.preview').html(textile($('.raw-contents').val()));

  // Set keyup bindings
  // $('.raw-contents').keyup(function() {
  //   $('.preview').html(textile($(this).val()));
  // })
  
});
